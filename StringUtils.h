#ifndef STRINGUTILS_H
#define STRINGUTILS_H

// Write the string src into dest starting at index
// Returns true if the string was written entierely false else
bool writeStringAt(String& dest, const String& src, size_t index);

#endif